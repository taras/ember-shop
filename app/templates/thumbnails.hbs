<!-- Thumbnails -->
<div class="large-8 columns">
  <div class="row">
      {{#each}}
        <div class="large-4 small-6 columns">
          <img {{bind-attr src=image}}>

          <div class="panel">
            <h5>{{title}}</h5>
            <h6 class="subheader">${{finalPrice}}</h6>
          </div>
        </div>
      {{/each}}
  </div>
</div>
<!-- End Thumbnails -->